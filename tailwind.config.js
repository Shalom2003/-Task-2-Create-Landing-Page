    /* @type {import('tailwindcss').Config} */
    module.exports = {
      content: [
        // ... your content paths
      ],
      theme: {
        extend: {
          fontFamily: {
            righteous: ['Righteous'], // 'sans-serif' as a fallback
          },
        },
      },
      plugins: [],
    };